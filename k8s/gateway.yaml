---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: mcp-service-restaurant-gatewayclass
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller

---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: mcp-service-restaurant-gateway
  namespace: default
spec:
  gatewayClassName: mcp-service-restaurant-gatewayclass
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All

    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - name: vishalk17.cloudwithme.dev-tls
      allowedRoutes:
        namespaces:
          from: All

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcp-service-restaurant-route
  namespace: default
spec:
  parentRefs:
    - name: mcp-service-restaurant-gateway
  hostnames:
    - "vishalk17.cloudwithme.dev"
  rules:
    # Catch-all route for the root path and everything else
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: mcp-service-restaurant
          port: 80


